import UIKit

/// Placeholder. The real intake lands in stage 4 — see the capture pipeline in
/// the handoff README. Until then this exists only so the target links and the
/// app has something to embed.
final class ShareViewController: UIViewController {
    override func viewDidLoad() {
        super.viewDidLoad()
        view.backgroundColor = .black
    }
}
